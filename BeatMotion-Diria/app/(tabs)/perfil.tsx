import { Button, View, Text } from "react-native";
import { ThemedView } from "@/components/themed-view";

const PerfilScreen = () => {
  return (
    <ThemedView>
      <Text className="text-lg font-bold text-white rounded-[1px]">Perfil</Text>
    </ThemedView>
  );
};

export default PerfilScreen;
